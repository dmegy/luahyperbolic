\directlua{t0 = os.clock()}
\documentclass[margin=.2cm,multi,tikz]{standalone}
%\usepackage{fontspec}
%\setmainfont{Libertinus Serif}
%\usepackage{mathtools,unicode-math}
%\setmainfont{Libertinus Math}
%\usepackage[dvipsnames]{xcolor}
\usepackage{luacode}
\usepackage{../luahyperbolic}

% not the angles : integers p, q , angles are PI/p and PI/q
\providecommand{\angleA}{2}
\providecommand{\angleB}{3}
\providecommand{\point}{B}
\providecommand{\depth}{28}


\begin{document}
\directlua{
A, B, C = hyper.triangleWithAngles(math.pi/\angleA,math.pi/\angleB,0)
local phi = hyper.automorphism(\point,0)
A,B,C = phi(A), phi(B), phi(C)
hyper.tikzBegin("scale=5")
hyper.fillTilingFromTriangle(A, B, C, \depth)
hyper.tikzEnd()
}

\begin{luacode*}
texio.write_nl(string.format("%.2f s: after tikz picture", os.clock() - t0))
\end{luacode*}
\end{document}